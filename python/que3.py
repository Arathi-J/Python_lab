#3.input a str and perform the following op
#   a.len of str
#   b.reverse a string
#   c.replace all occurance of the first character with dollar symbolexcept the first character
#   d. input another string concatinate the two str
